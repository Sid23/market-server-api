<p>Ciao <%= @resource.name %>!</p>

<p>Di seguito trovi il link per impostare la tua password.</p>

<p><%= link_to 'Imposta Password', edit_api_user_password_url(reset_password_token: @token, config: message['client-config'].to_s, redirect_url: "#{ENV['DEVISE_PASSWORD_RESET_URL']}") %> </p>

<p>Ricorda che la tua password è strettamente personale e non va divulgata<br/>
In caso di dubbi non esitare a contattare il nostro support<br/>
A presto e buon lavoro</p>
